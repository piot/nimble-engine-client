# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(nimble-engine-client STATIC 
  engine_client.c)

include(Tornado.cmake)
set_tornado(nimble-engine-client)

target_include_directories(nimble-engine-client PUBLIC ../include)


target_link_libraries(nimble-engine-client PUBLIC 
  rectify
  nimble-client
  time-tick)

